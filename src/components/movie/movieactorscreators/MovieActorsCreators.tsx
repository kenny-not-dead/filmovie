import { Title } from '../../title/Title'
import classes from './MovieActorsCreators.module.scss'

export const MovieActorsCreator = () => {
  return <div className={classes.movieActorsCreatorsContainer}></div>
}
